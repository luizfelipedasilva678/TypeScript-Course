let x = 10; // eslint-disable-line
x = 0b1010;
const y = 10;

let a = 100 as const; // eslint-disable-line

const pessoa = {
  nome: 'Luiz',
  sobrenome: 'Miranda',
};

function escolhaCor(cor: 'Vemelho' | 'Amarelo' | 'Azul') {
  return cor;
}

console.log(escolhaCor);

export default 1;
